Bot (extends entity):
	-> World
		-> entities
			-> location
			-> type (Player, creeper, bot)
		-> blocks
			-> location
			-> properies (sign with text, facing in direction...)
	-> Control methods

